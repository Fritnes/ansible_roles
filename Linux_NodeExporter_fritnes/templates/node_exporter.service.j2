[Unit]
Description=Node Exporter
Wants=network-online.target
After=network-online.target

[Service]
User={{ node_exporter_user }}
ExecStart={{ node_exporter_folder }}/node_exporter \
        --collector.textfile.directory={{ node_exporter_folder }}/textfile_collector

[Install]
WantedBy=default.target
