---
# tasks file for Ansible_Base
  - name: Dependonsi Debian
    include_tasks: "nft_debian.yml"
    when: ansible_os_family == "Debian"

  - name: Dependonsi Debian
    include_tasks: "f2b_debian.yml"
    when: ansible_os_family == "Debian"
